import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.unalone.mobile',
  appName: 'UnAlone',
  webDir: 'build',
  // During development, you can point to the dev server:
  // server: {
  //   url: 'http://192.168.1.10:3000', // replace with your machine IP
  //   cleartext: true,
  // },
};

export default config;
